<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Product Description</title>
  <style>
    /* General Body Styles */
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f8f9fa;
      margin: 0;
      padding: 0;
      color: #333;
    }

    /* Header styles */
    header {
      background-color: #343a40;
      color: white;
      padding: 10px 0;
      text-align: center;
    }

    header h1 {
      font-size: 2.5rem;
    }

    /* Container for product description */
    .product-container {
      max-width: 1200px;
      margin: 20px auto;
      padding: 20px;
      background-color: #fff;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      border-radius: 8px;
    }

    /* Title of product */
    .product-title {
      font-size: 2rem;
      font-weight: bold;
      color: #495057;
    }

    /* Product image styles */
    .product-image {
      width: 100%;
      height: auto;
      border-radius: 8px;
      margin-bottom: 20px;
    }

    /* Description section */
    .product-description {
      font-size: 1.1rem;
      line-height: 1.6;
      color: #6c757d;
      margin-bottom: 20px;
    }

    /* Price styling */
    .product-price {
      font-size: 1.5rem;
      font-weight: bold;
      color: #28a745;
    }

    /* Add to Cart Button */
    .add-to-cart-btn {
      display: inline-block;
      background-color: #007bff;
      color: white;
      padding: 10px 20px;
      text-decoration: none;
      border-radius: 5px;
      font-size: 1.1rem;
      transition: background-color 0.3s ease;
    }

    .add-to-cart-btn:hover {
      background-color: #0056b3;
    }

    /* Cart Table Styles */
    .cart-table-container {
      margin-top: 30px;
      background-color: #ffffff;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .cart-table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 20px;
    }

    .cart-table th, .cart-table td {
      padding: 12px;
      text-align: left;
      border: 1px solid #ddd;
    }

    .cart-table th {
      background-color: #343a40;
      color: white;
    }

    .cart-table td {
      background-color: #f8f9fa;
    }

    .cart-table td .remove-item {
      color: #dc3545;
      cursor: pointer;
      font-weight: bold;
    }

    .cart-table td .remove-item:hover {
      color: #721c24;
    }

    .footer {
      background-color: #343a40;
      color: white;
      text-align: center;
      padding: 10px 0;
      position: fixed;
      width: 100%;
      bottom: 0;
    }

    .footer p {
      margin: 0;
      font-size: 1rem;
    }

    /* Responsive Design */
    @media screen and (max-width: 768px) {
      .product-container {
        padding: 15px;
      }

      .product-title {
        font-size: 1.5rem;
      }

      .product-price {
        font-size: 1.2rem;
      }
    }
  </style>
</head>
<body>
  <header>
    <h1>Product Description</h1>
  </header>

  <div class="product-container">
    <h2 class="product-title">Ruby E-Commerce Product</h2>
    <img src="https://via.placeholder.com/800x400" alt="Product Image" class="product-image">
    <p class="product-description">This is an example of a product description that highlights all the important details. You can add features, benefits, and anything related to the product here. Make sure to give your users a detailed and informative overview of what they are purchasing.</p>
    <p class="product-price">$99.99</p>
    <a href="#" class="add-to-cart-btn">Add to Cart</a>

    <!-- Cart Table Section (Optional) -->
    <div class="cart-table-container">
      <%= render "cart_table", locals: { @cart_items => @cart.cart_items } %>
    </div>
  </div>

  <footer class="footer">
    <p>&copy; 2024 Ruby Shop. All Rights Reserved.</p>
  </footer>
</body>
</html>
